<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Confirm Recharge | ChainFlow</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <style>
        /* Global Styles */
        body {
            background-color: #FFFFFF;
            color: #000000;
            font-family: 'Poppins', sans-serif;
            margin: 0;
            padding: 0;
        }
        .container {
            max-width: 1200px;
            margin: auto;
            padding: 20px;
        }

        /* App Title */
        .app-title {
            text-align: center;
            font-size: 36px;
            font-weight: bold;
            margin-bottom: 20px;
            display: flex;
            justify-content: center;
            align-items: center;
            gap: 10px;
        }
        .app-title .icon {
            width: 60px;
            height: 60px;
            display: flex;
            justify-content: center;
            align-items: center;
            color: #000000;
            font-size: 40px;
        }

        /* Section Styles */
        .section {
            margin: 40px auto;
            padding: 25px;
            background: #F8F8F8;
            border-radius: 15px;
            box-shadow: 0px 6px 12px rgba(0, 0, 0, 0.1);
            max-width: 600px;
        }
        .section-title {
            font-size: 24px;
            margin-bottom: 30px;
            color: black;
            font-weight: 600;
            text-align: center;
            text-transform: uppercase;
        }

        /* Details Container */
        .details-container {
            background: #FFFFFF;
            padding: 20px;
            border-radius: 10px;
            margin-bottom: 25px;
            box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.05);
        }
        .detail-item {
            display: flex;
            justify-content: space-between;
            padding: 12px 0;
            border-bottom: 1px solid #F0F0F0;
        }
        .detail-item:last-child {
            border-bottom: none;
        }
        .detail-label {
            font-weight: 600;
            color: #666;
        }
        .detail-value {
            color: #2973B2;
            font-weight: 500;
        }

        /* Type Selection */
        .type-selection {
            display: flex;
            justify-content: center;
            gap: 30px;
            margin: 25px 0;
        }
        .radio-option {
            display: flex;
            align-items: center;
            gap: 8px;
            cursor: pointer;
            padding: 10px 20px;
            background: #FFFFFF;
            border-radius: 8px;
            box-shadow: 0px 2px 6px rgba(0, 0, 0, 0.1);
            transition: 0.3s;
        }
        .radio-option:hover {
            transform: translateY(-2px);
            box-shadow: 0px 4px 8px rgba(41, 115, 178, 0.2);
        }
        .radio-option input[type="radio"] {
            margin-right: 8px;
        }
        .radio-option span {
            font-weight: 500;
        }

        /* Button Container */
        .button-container {
            display: flex;
            justify-content: center;
            gap: 20px;
            margin-top: 30px;
        }
        .button {
            display: flex;
            align-items: center;
            gap: 8px;
            padding: 12px 24px;
            border: none;
            border-radius: 10px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            transition: 0.3s;
            text-decoration: none;
        }
        .home-button {
            background-color: #F0F0F0;
            color: #000000;
        }
        .button[type="submit"] {
            background-color: #2973B2;
            color: #FFFFFF;
        }
        .button:hover {
            transform: scale(1.05);
            box-shadow: 0px 4px 15px rgba(41, 115, 178, 0.3);
        }

        /* Mobile Responsiveness */
        @media (max-width: 768px) {
            .container {
                padding: 10px;
            }
            .section {
                margin: 20px auto;
                padding: 15px;
            }
            .type-selection {
                flex-direction: column;
                gap: 15px;
            }
            .button-container {
                flex-direction: column;
            }
            .button {
                width: 100%;
                justify-content: center;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="app-title">
            <div class="icon"><i class="fas fa-university"></i></div>
            <span>ChainFlow</span>
        </div>
        
        <div class="section">
            <div class="section-title">Confirm Recharge</div>
            
            <div class="details-container">
                <% Object.keys(details).forEach(key => { %>
                    <% if (key !== "upi_id") { %>
                        <div class="detail-item">
                            <span class="detail-label"><%= key.charAt(0).toUpperCase() + key.slice(1).replace('_', ' ') %>:</span>
                            <span class="detail-value"><%= details[key] %></span>
                        </div>
                    <% } %>
                <% }); %>
            </div>

            <form action="/process-mobile-payment" method="POST">
                <% Object.keys(details).forEach(key => { %>
                    <input type="hidden" name="<%= key %>" value="<%= details[key] %>">
                <% }); %>
                
                <div class="type-selection">
                    <label class="radio-option">
                        <input type="radio" name="type" value="global" checked>
                        <span>Global</span>
                    </label>
                    <label class="radio-option">
                        <input type="radio" name="type" value="private">
                        <span>Private</span>
                    </label>
                </div>

                <div class="button-container">
                    <a href="/home" class="button home-button">
                        <i class="fas fa-home"></i>
                        Home
                    </a>
                    <button type="submit" class="button">
                        <i class="fas fa-check-circle"></i>
                        Click to Pay
                    </button>
                </div>
            </form>
        </div>
    </div>
</body>
</html>