<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Electricity Bill Payment | ChainFlow</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <style>
        /* Global Styles */
        body {
            background-color: #FFFFFF;
            color: #000000;
            font-family: 'Poppins', sans-serif;
            margin: 0;
            padding: 0;
        }
        .container {
            max-width: 1200px;
            margin: auto;
            padding: 20px;
        }

        /* App Title */
        .app-title {
            text-align: center;
            font-size: 36px;
            font-weight: bold;
            margin-bottom: 20px;
            display: flex;
            justify-content: center;
            align-items: center;
            gap: 10px;
        }
        .app-title .icon {
            width: 60px;
            height: 60px;
            display: flex;
            justify-content: center;
            align-items: center;
            color: #000000;
            font-size: 40px;
        }

        /* Section Styles */
        .section {
            margin: 40px auto;
            padding: 25px;
            background: #F8F8F8;
            border-radius: 15px;
            box-shadow: 0px 6px 12px rgba(0, 0, 0, 0.1);
            max-width: 600px;
        }
        .section-title {
            font-size: 24px;
            margin-bottom: 30px;
            color: black;
            font-weight: 600;
            text-align: center;
            text-transform: uppercase;
        }

        /* Form Styles */
        .form-container {
            background: #FFFFFF;
            padding: 25px;
            border-radius: 10px;
            box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.05);
        }
        .input-group {
            margin-bottom: 20px;
        }
        .input-group label {
            display: block;
            margin-bottom: 8px;
            font-weight: 500;
            color: #666;
        }
        .input-group input {
            width: 100%;
            padding: 12px;
            border: 1px solid #E0E0E0;
            border-radius: 8px;
            font-size: 16px;
            transition: 0.3s;
        }
        .input-group input:focus {
            outline: none;
            border-color: #2973B2;
            box-shadow: 0 0 0 2px rgba(41, 115, 178, 0.2);
        }

        /* Error Message */
        .error-message {
            background-color: #FFE9E9;
            color: #D63031;
            padding: 12px;
            border-radius: 8px;
            margin-bottom: 20px;
            text-align: center;
            font-weight: 500;
        }

        /* Button Container */
        .button-container {
            display: flex;
            justify-content: center;
            gap: 20px;
            margin-top: 30px;
        }
        .button {
            display: flex;
            align-items: center;
            gap: 8px;
            padding: 12px 24px;
            border: none;
            border-radius: 10px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            transition: 0.3s;
            text-decoration: none;
        }
        .home-button {
            background-color: #F0F0F0;
            color: #000000;
        }
        .submit-button {
            background-color: #2973B2;
            color: #FFFFFF;
        }
        .button:hover {
            transform: scale(1.05);
            box-shadow: 0px 4px 15px rgba(41, 115, 178, 0.3);
        }

        /* Mobile Responsiveness */
        @media (max-width: 768px) {
            .container {
                padding: 10px;
            }
            .section {
                margin: 20px auto;
                padding: 15px;
            }
            .button-container {
                flex-direction: column;
            }
            .button {
                width: 100%;
                justify-content: center;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="app-title">
            <div class="icon"><i class="fas fa-university"></i></div>
            <span>ChainFlow</span>
        </div>
        
        <div class="section">
            <div class="section-title">
                <i class="fas fa-bolt"></i> Electricity Bill Payment
            </div>
            
            <div class="form-container">
                <% if (locals.message) { %>
                    <div class="error-message">
                        <%= message %>
                    </div>
                <% } %>

                <form action="/electricity-bill-payment" method="post">
                    <div class="input-group">
                        <label for="consumer-number">Consumer Number</label>
                        <input type="text" id="consumer-number" name="consumer-number" required>
                    </div>

                    <input type="hidden" name="authorityName" value="<%= authorityName %>">

                    <div class="input-group">
                        <label for="nickname">Nickname (Optional)</label>
                        <input type="text" id="nickname" name="nickname">
                    </div>

                    <div class="button-container">
                        <a href="/home" class="button home-button">
                            <i class="fas fa-home"></i>
                            Home
                        </a>
                        <button type="submit" class="button submit-button">
                            <i class="fas fa-arrow-right"></i>
                            Proceed
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</body>
</html>