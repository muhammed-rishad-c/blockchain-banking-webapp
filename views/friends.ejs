<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Friends List | ChainFlow</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
  <style>
    /* Global Styles */
    body {
      background-color: #FFFFFF;
      color: #000000;
      font-family: 'Poppins', sans-serif;
      margin: 0;
      padding: 0;
    }
    .container {
      max-width: 1200px;
      margin: auto;
      padding: 20px;
    }

    /* App Title */
    .app-title {
      text-align: center;
      font-size: 36px;
      font-weight: bold;
      margin-bottom: 20px;
      display: flex;
      justify-content: center;
      align-items: center;
      gap: 10px;
    }
    .app-title .icon {
      width: 60px;
      height: 60px;
      display: flex;
      justify-content: center;
      align-items: center;
      color: #000000;
      font-size: 40px;
    }

    /* Section Styles */
    .section {
      margin: 40px auto;
      padding: 25px;
      background: #F8F8F8;
      border-radius: 15px;
      box-shadow: 0px 6px 12px rgba(0, 0, 0, 0.1);
    }
    .section-title {
      font-size: 24px;
      margin-bottom: 30px;
      color: black;
      font-weight: 600;
      text-transform: uppercase;
      text-align: center;
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 10px;
    }
    .section-title i {
      color: #2973B2;
    }

    /* Friends List */
    .friends-container {
      display: flex;
      flex-direction: column;
      gap: 20px;
    }
    .friend-card {
      background: white;
      border-radius: 12px;
      padding: 20px;
      display: flex;
      align-items: center;
      gap: 20px;
      box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.05);
      transition: 0.3s;
    }
    .friend-card:hover {
      transform: translateY(-2px);
      box-shadow: 0px 6px 15px rgba(41, 115, 178, 0.1);
    }
    .friend-photo {
      width: 80px;
      height: 80px;
      border-radius: 50%;
      object-fit: cover;
      border: 3px solid #2973B2;
    }
    .friend-info {
      flex-grow: 1;
    }
    .friend-name {
      font-size: 18px;
      font-weight: 600;
      margin-bottom: 5px;
      color: #2973B2;
    }
    .friend-upi {
      color: #666;
      font-size: 14px;
    }

    /* Button Styles */
    .button-container {
      display: flex;
      gap: 15px;
      justify-content: center;
      margin-top: 30px;
    }
    .button {
      padding: 12px 24px;
      border-radius: 10px;
      font-weight: bold;
      text-decoration: none;
      display: flex;
      align-items: center;
      gap: 8px;
      transition: 0.3s;
      border: none;
      cursor: pointer;
      font-size: 16px;
    }
    .home-button {
      background-color: #F0F0F0;
      color: #000000;
    }
    .pay-button {
      background-color: #2973B2;
      color: #FFFFFF;
      padding: 12px 30px;
    }
    .button:hover {
      transform: scale(1.05);
      box-shadow: 0px 4px 15px rgba(0, 0, 0, 0.1);
    }
    .pay-button:hover {
      background-color: #2973B2;
      box-shadow: 0px 4px 15px rgba(41, 115, 178, 0.3);
    }

    /* Empty State */
    .empty-state {
      text-align: center;
      padding: 40px;
      font-size: 18px;
      color: #666;
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 15px;
    }
    .empty-state i {
      font-size: 48px;
      color: #2973B2;
    }

    /* Mobile Responsiveness */
    @media (max-width: 768px) {
      .container {
        padding: 15px;
      }
      .friend-card {
        flex-direction: column;
        text-align: center;
      }
      .friend-photo {
        width: 100px;
        height: 100px;
      }
      .button-container {
        flex-direction: column;
        width: 100%;
      }
      .button {
        width: 100%;
        justify-content: center;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="app-title">
      <div class="icon"><i class="fas fa-university"></i></div>
      <span>ChainFlow</span>
    </div>

    <div class="section">
      <div class="section-title">
        <i class="fas fa-user-friends"></i>
        Friends List
      </div>

      <% if (friends && friends.length > 0) { %>
        <div class="friends-container">
          <% friends.forEach(friend => { %>
            <div class="friend-card">
              <img src="/uploads/<%= friend.photo %>" alt="Friend Photo" class="friend-photo">
              <div class="friend-info">
                <div class="friend-name"><%= friend.full_name %></div>
                <div class="friend-upi"><%= friend.upi %></div>
              </div>
              <button class="button pay-button" onclick="window.location.href='/pay-friend/<%= friend.upi %>'">
                <i class="fas fa-money-bill-wave"></i>
                Pay
              </button>
            </div>
          <% }); %>
        </div>
      <% } else { %>
        <div class="empty-state">
          <i class="fas fa-user-friends"></i>
          <div>No friends found in your list</div>
        </div>
      <% } %>

      <div class="button-container">
        <a href="/home" class="button home-button">
          <i class="fas fa-home"></i>
          Back to Home
        </a>
      </div>
    </div>
  </div>
</body>
</html>