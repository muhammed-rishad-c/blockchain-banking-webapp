<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>ChainFlow</title>
  <script src="https://kit.fontawesome.com/a076d05399.js" crossorigin="anonymous"></script>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
  <style>
    /* Global Styles */
    body {
      background-color: #FFFFFF; /* White background */
      color: #000000; /* Black text */
      font-family: 'Poppins', sans-serif;
      margin: 0;
      padding: 0;
    }
    .container {
      max-width: 1200px;
      margin: auto;
      padding: 20px;
    }

    /* Header */
    .header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 20px;
      background: #F0F0F0; /* Light gray */
      color: black;
      border-radius: 15px;
      box-shadow: 0px 5px 15px rgba(0, 0, 0, 0.1);
    }
    .user-info {
      display: flex;
      align-items: center;
    }
    .user-info img {
      height: 110px; 
      width: 110px;
      border-radius: 8px; 
      margin-right: 15px;
      transition: 0.3s;
    }
    .user-info img:hover {
      transform: scale(1.1); 
      box-shadow: 0px 0px 15px rgba(0, 0, 0, 0.5);
    }
    .user-info h1 {
      font-size: 24px; /* Larger font size */
      font-weight: 600;
      text-transform: capitalize; 
    }
    .logout-button a {
      padding: 12px 24px;
      background-color: #2973B2; /* Updated to specific blue color */
      color: #FFFFFF; /* White */
      text-decoration: none;
      border-radius: 10px;
      font-weight: bold;
      transition: 0.3s;
      box-shadow: 0px 4px 10px rgba(41, 115, 178, 0.3);
      display: flex;
      align-items: center;
    }
    .logout-button a i {
      margin-right: 8px;
    }
    .logout-button a:hover {
      background-color: #2973B2; /* Consistent blue color */
      box-shadow: 0 0 20px rgba(41, 115, 178, 0.6);
      transform: scale(1.05);
    }

    /* Title */
    .app-title {
      text-align: center;
      font-size: 36px; /* Larger font size for the title */
      font-weight: bold;
      margin-bottom: 20px;
      display: flex;
      justify-content: center;
      align-items: center;
      gap: 10px;
    }
    .app-title .icon {
      width: 60px;
      height: 60px;
      display: flex;
      justify-content: center;
      align-items: center;
      color: #000000; /* Black color for the icon */
      font-size: 40px; /* Larger icon size */
    }

    /* Section Styles */
    .section {
      margin: 40px auto;
      padding: 25px;
      background: #F8F8F8; /* Very light gray */
      border-radius: 15px;
      box-shadow: 0px 6px 12px rgba(0, 0, 0, 0.1);
    }
    .section-title {
      font-size: 20px; /* Slightly smaller font size */
      margin-bottom: 20px;
      color: black;
      font-weight: 600;
      text-transform: uppercase;
      text-align: center; 
    }

    /* Button Styles */
    .button-container {
      display: flex;
      flex-wrap: wrap;
      justify-content: center;
      gap: 20px;
      margin-top: 15px;
    }
    .button-container a {
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      text-decoration: none;
      padding: 12px 18px; /* Adjusted padding */
      background: #FFFFFF; /* White */
      color: #000000; /* Black */
      border-radius: 12px;
      font-size: 16px; /* Slightly smaller font size */
      font-weight: bold;
      width: 180px; /* Adjusted width */
      height: 90px; /* Adjusted height */
      transition: 0.3s;
      box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.1);
    }
    .button-container a:hover {
      background: #2973B2; /* Specific blue color */
      color: white;
      transform: scale(1.05);
      box-shadow: 0px 4px 20px rgba(41, 115, 178, 0.4);
    }
    .button-container i {
      font-size: 24px; /* Slightly smaller icon size */
      margin-bottom: 5px;
    }

    /* Mobile Responsiveness */
    @media (max-width: 768px) {
      .header {
        flex-direction: column;
        align-items: flex-start;
      }
      .button-container {
        flex-direction: column;
        align-items: center;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="app-title">
      <div class="icon"><i class="fas fa-university"></i></div> <!-- Banking icon -->
      <span>ChainFlow</span>
    </div>
    <% if (userDetail && userDetail.length > 0) { %>
      <% userDetail.forEach(user => { %>
        <div class="header">
          <div class="user-info">
            <a href="/user-profile/<%= user.upid %>">
              <img src="/uploads/<%= user.photo %>" alt="User Photo">
            </a>
            <h1>Welcome  <%= user.fullname.charAt(0).toUpperCase() + user.fullname.slice(1).toLowerCase() %></h1>
          </div>
          <div class="logout-button">
            <a href="/logout"><i class="fas fa-sign-out-alt"></i> Logout</a>
          </div>
        </div>
        <div class="section">
          <div class="section-title">Transfer Options</div>
          <div class="button-container">
            <a href="/transfer/<%= user.upid %>">
              <i class="fas fa-exchange-alt"></i>
              <span>Transfer</span>
            </a>
            <a href="/pay-any-upi/<%= user.upid %>">
              <i class="fas fa-qrcode"></i>
              <span>UPI Pay</span>
            </a>
            <a href="/pay-contact/<%= user.upid %>">
              <i class="fas fa-user-friends"></i>
              <span>Pay Contact</span>
            </a>
            <a href="/bank-transfer/<%= user.upid %>">
              <i class="fas fa-university"></i>
              <span>Bank Transfer</span>
            </a>
          </div>
        </div>
        <div class="section">
          <div class="section-title">Bill Payments</div>
          <div class="button-container">
            <a href="/electricity-bill/<%= user.upid %>">
              <i class="fas fa-bolt"></i>
              <span>Electricity</span>
            </a>
            <a href="/water-bill/<%= user.upid %>">
              <i class="fas fa-tint"></i>
              <span>Water</span>
            </a>
            <a href="/mobile-recharge/<%= user.upid %>">
              <i class="fas fa-mobile-alt"></i>
              <span>Mobile</span>
            </a>
          </div>
        </div>
        <div class="section">
          <div class="section-title">Account Services</div>
          <div class="button-container">
            <a href="/balance/<%= user.upid %>">
              <i class="fas fa-wallet"></i>
              <span>Balance</span>
            </a>
            <a href="/search/<%= user.upid %>">
              <i class="fas fa-search"></i>
              <span>Search</span>
            </a>
            <a href="/history/<%= user.upid %>">
              <i class="fas fa-history"></i>
              <span>History</span>
            </a>
          </div>
        </div>
      <% }); %>
    <% } else { %>
      <div>No user details found.</div>
    <% } %>
  </div>
</body>
</html>