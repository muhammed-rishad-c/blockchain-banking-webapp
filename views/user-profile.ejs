<!DOCTYPE html>
<html>
<head>
  <title>User Profile</title>
  <style>
    .profile-container {
      display: flex;
      flex-direction: column;
      align-items: center;
      margin-top: 50px;
    }
    .profile-photo {
      height: 100px;
      width: 100px;
      border-radius: 50%;
    }
    .profile-details {
      margin-top: 20px;
      text-align: center;
    }
    .profile-details div {
      margin-bottom: 10px;
    }
    .logout-button, .home-button, .change-photo-button {
      margin-top: 10px;
      padding: 10px 20px;
      background-color: #007bff;
      color: white;
      border-radius: 5px;
      text-decoration: none;
    }
    .logout-button:hover, .home-button:hover, .change-photo-button:hover {
      background-color: #0056b3;
    }
    .home-button {
      background-color: #28a745;
    }
    .home-button:hover {
      background-color: #218838;
    }
    .logout-button {
      background-color: #ff0000;
    }
    .logout-button:hover {
      background-color: #cc0000;
    }
  </style>
</head>
<body>
  <% if (user) { %>
    <div class="profile-container">
      <img src="/uploads/<%= user.photo %>" alt="User Photo" class="profile-photo">
      <div class="profile-details">
        <div><strong>Full Name:</strong> <%= user.fullname %></div>
        <div><strong>Username:</strong> <%= user.username %></div>
        <div><strong>Phone Number:</strong> <%= user.phonenumber %></div>
        <div><strong>UPID:</strong> <%= user.upid %></div>
      </div>
      <a href="/home" class="home-button">Home</a>
      <a href="/logout" class="logout-button">Logout</a>
    </div>
  <% } else { %>
    <div>No user details found.</div>
  <% } %>
</body>
</html>
